
примеры импользования аннотаций
@Primary (дает приоритет при инжекции бина, если существует несколько кандидатов)
@Order (задает порядок инициализации бинов в контексте)
@DependsOn (гарантирует, что указанные в аннотации бины будут созданы до создания бина с этой аннотацией)
@Autowired (связывает ссылку и бин по типу)
@Qualifier (привязывает конкретный класс через @Autowired)
@Value (используется на уровне поля или метода и указывает либо значение по умолчанию, либо значение из конфигурационного файла)
@Import (добавляет бины в контекст из внешней конфигурации)
@ComponentScan (@ComponentScan annotation along with @Configuration annotation to specify the packages that we want to be scanned)
